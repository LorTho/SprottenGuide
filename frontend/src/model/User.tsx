export type User = {
    id: string,
    firstName: string,
    lastName: string,
}

export type Time = {
    day: string,
    startTime: string,
}

export type DtoUser = {
    memberCode: string,
    firstName: string,
    lastName: string,
}
